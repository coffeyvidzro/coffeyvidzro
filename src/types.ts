export type Site = {
  name: string;
  email: string;
  url: string;
};

export type Metadata = {
  title: string;
  description: string;
};

export type Social = {
  name: string;
  href: string;
};
