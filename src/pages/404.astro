---
import { SITE } from "../consts";
import IndexLayout from "../layouts/IndexLayout.astro";
---

<IndexLayout title={`404 - ${SITE.name}`} description="Not Found">
  <style>
    .error-container {
      color: var(--text-secondary);
    }
  </style>
  <p class="error-container">Page Not Found…</p>
</IndexLayout>
